server:
  port: 8084

spring:
  application:
    name: bill-service
  datasource:
    url: jdbc:mysql://localhost:3310/db_bill
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
