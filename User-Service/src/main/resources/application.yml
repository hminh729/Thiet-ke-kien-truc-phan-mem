server:
  port: 8087 

spring:
  application:
    name: USER-SERVICE
  datasource:
    url: jdbc:mysql://localhost:3312/db_user
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
